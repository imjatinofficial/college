#we have to write all these commands in the IDLE shell to get the result

a = 10 #taking a variable 
type(a) #printing the type of variable a 
b=("This is a string") #taking string
type(b) #printing the type of the variable b
c=4.3456 #taking the variable as float
type(c) #printing the type of the variable c
