# creating function in python

def name(): #defining the function by using the def keyword in the starting
    enter_name = input('Enter your name : ') # asking input
    return enter_name # returning the value after the function is completed
print(name()) #calling the function
